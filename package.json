{
  "name": "contentbuilder",
  "version": "0.1.0",
  "private": true,
  "description": "On-brand Instagram carousel & story asset generator (internal tool)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n db,api,web -c green,blue,magenta \"npm:db\" \"npm:dev:api\" \"npm:dev:web\"",
    "db": "node scripts/start-mongo.mjs",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm run build --workspace apps/api && npm run build --workspace apps/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "fonts": "node scripts/copy-fonts.mjs",
    "seed": "npm --prefix apps/api run seed",
    "eval:compose": "npm --prefix apps/api run eval:compose --",
    "migrate:photos": "npm --prefix apps/api run migrate:photos --",
    "migrate:media-urls": "npm --prefix apps/api run migrate:media-urls --",
    "lint": "eslint .",
    "migrate:recipes": "npm --prefix apps/api run migrate:recipes --"
  },
  "devDependencies": {
    "@types/supertest": "^7.2.0",
    "concurrently": "^9.1.0",
    "eslint": "^10.6.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-unused-imports": "^4.4.1",
    "mongodb-memory-server": "^11.2.0",
    "supertest": "^7.2.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.62.1",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
